# A1-PILOT-002：一个小矩形的严格零点计数

日期：2026-09-16。方向 `RC:A1`；当前位置 `RC:E1`；本轮状态 **END**。
执行完成，不代表 A1 方向结束，也不代表达到反例确认节点 E2。

## 问题与结果

上一轮 Newton 初值会进入不同吸引域。本轮改用整段边界区间包围和辐角原理，
检验一个固定区域，而不依赖求根初值。配置在运行前写入
[协议](A1_PILOT_002_PROTOCOL.md)，没有依据结果调整区域或预算。

| 实际 ζ 区域 | 精度（十进制位） | 认证零点数（计重数） | 函数求值次数 | 用时（秒） |
| --- | --- | --- | --- | --- |
| `[0.5001,0.70] × [10010,10011]` | 40 | 0 | 17135 | 16.18 |
| 同一线外目标 | 70 | 0 | 17363 | 28.55 |
| `[0.49,0.51] × [10010,10011]` | 40 | 2 | 13493 | 15.09 |
| 同一跨线对照 | 70 | 2 | 13682 | 22.00 |

主实验 **4/4 完成、0 失败、0 未决、0 线外候选矩形**，整轮 81.94 秒。
两个精度下目标边界也认证不含零，因此闭目标矩形内无零点。
跨线对照含两个零点，但仅凭此计数不能证明它们恰在临界线上。
这两个精度使用同一算法和 FLINT 库，不能称为两个独立实现的交叉证明。

## 校准与解释边界

运行前 9/9 校准符合预期：4 项认证计数、2 项未决、3 项拒绝。
其中合成线外根仅为模型正例；边界含零、预算不足、极点、浮点坐标和无效矩形
均未被错误转换为“零点数 0”。另有 7 项单元测试通过（本轮 2 项、前轮 5 项），
检查断裂边界、失效包围标记和未决结果不能提升为排除结论等情况；Python 3 语法检查通过。

这是 **实际 ζ 的 Arb/FLINT 区间计数**，区别于有限点采样、surrogate 对照及未经认证的求根端点。
算法依据见[计数器审计协议](A1_PILOT_002_PROTOCOL.md)及其中的主要文献链接。
结论依赖经审计代码和 FLINT/Arb 的正确性；存下来的区间显示字符串不是可独立验证的形式化证明对象。
有些显示字符串因格式舍入看似包含零，不能用它们替代内存中的区间判定。

未覆盖 `0.5 < sigma < 0.5001`、`sigma > 0.70` 或其他高度。
不排除整个高度带、其他方向或 RH 反例；本轮也没有新异常线索。
其价值是把上一轮有限初值的阴性结果，推进成一个明确、可复算的局部排除。

## 复现与留档

- Python 3.14.3、python-flint 0.9.0；本机没有 Python 3.10。
- 每项预算：深度 26、函数求值 30000 次、软时间上限 60 秒。
- 主目录：`C:/Users/wangl/Documents/rh_counter/rh_counterexample/runs/a1-rectangle-probe-pilot-20260916`。
- 校准目录：`C:/Users/wangl/Documents/rh_counter/rh_counterexample/runs/a1-rectangle-probe-smoke-20260916`。
- 主 run ID：`c8f3e5e64c72c443d4ab17eab5dedd2b35a95a33c7c76c8a92855c2f803cb873`。
- [完整证据包](A1_PILOT_002_EVIDENCE.json)含配置、源文件指纹、逐任务数据、边界记录及文件哈希。
- [可恢复 runner](a1_rectangle_probe_batch_py3.py)保留逐任务 JSON，并立即更新 progress；源代码或配置变化时拒绝混用旧结果。
- 在主运行记录副本上验证恢复：保留 4 项、重新计算 0 项，逐任务 SHA-256 全部不变。
- 本轮前台运行退出码 0；没有另存 stderr 文件。实验进程已结束，无待运行任务。

在仓库根目录使用 `py -3.14` 调用 runner，先 `--profile smoke --output-dir <绝对目录>`，
再 `--profile pilot --output-dir <另一绝对目录> --smoke-dir <校准绝对目录>`。

## 下一项明确问题

不建议仅把同类空矩形无限铺开。一个尚未启动的后续问题是：对两个已知数值端点分别构造
关于临界线对称且互不相交的小矩形，并各认证恰有一个零点。
若成功，结合 ζ 零点的 `rho -> 1-conjugate(rho)` 对称性，可把“近线端点”提升为
这两个局部零点在线上的结论；必须重新冻结区域、预算和验证条件。
这是一项认证能力建设，不应冒充异常发现。继续寻找反例时，另需选择有明确异常指标的发现实验。
